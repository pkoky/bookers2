<%= render 'users/list', user: @user, book: @book %>



<% @users.each do |user| %>
<divind>
  <p><%= user.name %></p>
  <%= link_to user_path(user.id) do %>
  <p>Show</p>
  <% end %>
</div>


<% end %>