<%= render 'users/list', user: @user, book: @book %>


<% @books.each do |book| %>
<div>
  <p><%= book.user.name %></p>
  <%= link_to book_path(book.id) do %>
  <p><%= book.title %></p>
  <% end %>
  <p><%= book.body %></p>
</div>
<% end %>